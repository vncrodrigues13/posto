package UI;

import java.io.FileNotFoundException;
import posto.exceptions.*;


public class test {
    public static void main(String[] args) throws invalidItem, invalidPrice, invalidQtdItens, FileNotFoundException, naoProduto, naoCombustivel, noHistory, invalidLogin, invalidAccess {
        user userUi = new user();
    }
}
